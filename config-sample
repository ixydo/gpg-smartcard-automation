#
# This configuration will be used when generating your keys
#

###
# User ID
###
GPG_REAL_NAME="Real Name"
GPG_COMMENT="https://keybase.io/userid"
GPG_EMAIL="email@addr.ess"

# Use the appropriate GPG_USER_ID line below as needed depending on which
# combination of above values is set.

#GPG_USER_ID="${GPG_EMAIL}"
#GPG_USER_ID="${GPG_REAL_NAME} <${GPG_EMAIL}>"
GPG_USER_ID="${GPG_REAL_NAME} (${GPG_COMMENT}) <${GPG_EMAIL}>"

###
# Key parameters
###
MASTER_ALGO="rsa4096"
MASTER_VALIDITY="5y"

SUBKEY_ALGO="rsa2048"
SUBKEY_VALIDITY="2y"

